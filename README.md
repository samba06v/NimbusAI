# Cloud Optimizer Agent

A workspace for the Cloud Optimizer Agent project.

## Scripts

- `dev`: Run the development environment.
- `build`: Build the project.
- `typecheck`: Run TypeScript type checking.

## Usage

Use pnpm to install dependencies and run scripts.
